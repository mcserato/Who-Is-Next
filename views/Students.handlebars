<div class="container">
    <div class="row">
    	{{#each students}}
    	<div class="col">
	        <a href="/api/student/{{"student_number"}}">
	            <img src="{{"picture"}}" class="circle responsive-img" title="{{"first_name"}} {{"last_name"}}">
	            <p>{{"first_name"}} {{"last_name"}}</p>
	        </a>
        </div>
        {{/each}}
    </div>
</div>

